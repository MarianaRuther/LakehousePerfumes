.DS_Store
node_modules/
client/dist/
dist/
build/
.env
.databricks/
.smoke-test/
test-results/
playwright-report/

# Auto-generated types (endpoint-specific, varies per developer)
shared/appkit-types/serving.d.ts


# helper copy of the repo-root SQL-guard hook (its command is cwd-relative,
# so it must resolve when a tool runs with cwd inside this subproject)
.claude/
